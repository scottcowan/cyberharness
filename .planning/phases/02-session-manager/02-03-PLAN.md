---
phase: 02-session-manager
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - packages/client/src/cyberharness/session/manager.py
  - packages/client/src/cyberharness/session/titler.py
  - packages/client/src/cyberharness/session/__init__.py
  - packages/client/src/cyberharness/tui/screens/resume.py
  - packages/client/src/cyberharness/tui/app.py
  - packages/client/tests/unit/session/test_manager.py
  - packages/client/tests/unit/session/test_resume.py
autonomous: true
requirements: [SESS-03]

must_haves:
  truths:
    - "On startup the boot sequence calls SessionStore.scan_active(); zero results skip ResumeScreen and create a new session; one or more results push ResumeScreen (D-16, D-17)"
    - "ResumeScreen is a Textual `ModalScreen[ResumeResult]` where ResumeResult is TypedDict({action: Literal['resume','fresh'], session_id: str|None}); it calls `self.dismiss(result)` — never `await self.dismiss()` (RESEARCH.md Pattern 3)"
    - "'Start fresh' invokes SessionStore.abandon_all on every listed session, then creates a new one (D-15, Pitfall 4)"
    - "SessionMgr.add_user_turn calls store.save BEFORE any router.stream call — user turn is on disk before the model is invoked (D-07)"
    - "Auto-title task is fire-and-forget via asyncio.create_task, tracked in SessionMgr._bg_tasks so shutdown() can cancel; failure to title never crashes the session (Pitfall 6, Pitfall 11)"
    - "Phase 2 titler is a stub that returns `session.messages[0].content[:50]` — no Ollama call in Phase 2 (RESEARCH.md Open Question 3)"
  artifacts:
    - path: "packages/client/src/cyberharness/session/manager.py"
      provides: "SessionMgr with add_user_turn, add_assistant_turn, shutdown; owns bg tasks and in-process ordering"
      contains: "class SessionMgr"
      exports: ["SessionMgr"]
    - path: "packages/client/src/cyberharness/session/titler.py"
      provides: "generate_title(session_id) stub returning first-message excerpt; real Ollama call lands in Phase 3"
      contains: "async def generate_title"
    - path: "packages/client/src/cyberharness/tui/screens/resume.py"
      provides: "ResumeScreen(ModalScreen[ResumeResult]) + ResumeResult TypedDict"
      contains: "class ResumeScreen"
      exports: ["ResumeScreen", "ResumeResult"]
    - path: "packages/client/src/cyberharness/tui/app.py"
      provides: "boot sequence hook wiring scan_active → ResumeScreen? → MainScreen"
      contains: "scan_active"
  key_links:
    - from: "packages/client/src/cyberharness/tui/app.py"
      to: "packages/client/src/cyberharness/session/store.py"
      via: "scan_active() at App.on_mount before push_screen"
      pattern: "scan_active"
    - from: "packages/client/src/cyberharness/tui/screens/resume.py"
      to: "packages/client/src/cyberharness/session/store.py"
      via: "LoadedSession list passed to ResumeScreen constructor; abandon_all called from callback"
      pattern: "abandon_all"
    - from: "packages/client/src/cyberharness/session/manager.py"
      to: "packages/client/src/cyberharness/session/titler.py"
      via: "asyncio.create_task(titler.generate(session_id)) after first assistant message"
      pattern: "create_task"
---

<objective>
Land the runtime session layer: SessionMgr (turn ordering + auto-title task tracking), the Phase-2 stub titler, ResumeScreen ModalScreen, and the tui/app.py boot hook that sequences scan_active → ResumeScreen? → MainScreen. Satisfies SESS-03 (resume prompt) and completes the SESS-01 runtime behaviour (history accumulates across turns via SessionMgr, not just via the model).

Purpose: Phase 3 (router) needs `SessionMgr.add_user_turn` + `add_assistant_turn` to hook into the streaming turn lifecycle; Phase 5 (TUI) needs the boot sequence to already resolve to an active session before MainScreen mounts.

Output: `pytest tests/unit/session/` GREEN end-to-end; `App.run_test` pilot boots with zero sessions and reaches MainScreen; with one interrupted session the ResumeScreen is pushed; "Start fresh" abandons all listed sessions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-session-manager/02-CONTEXT.md
@.planning/phases/02-session-manager/02-RESEARCH.md
@.planning/phases/02-session-manager/02-VALIDATION.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/02-session-manager/02-01-SUMMARY.md
@.planning/phases/02-session-manager/02-02-SUMMARY.md
@packages/client/src/cyberharness/session/store.py
@packages/client/src/cyberharness/session/models.py
@packages/client/src/cyberharness/tui/app.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SessionMgr + titler stub — turn API and fire-and-forget auto-title</name>
  <files>
    packages/client/src/cyberharness/session/manager.py,
    packages/client/src/cyberharness/session/titler.py,
    packages/client/src/cyberharness/session/__init__.py,
    packages/client/tests/unit/session/test_manager.py
  </files>
  <read_first>
    .planning/phases/02-session-manager/02-RESEARCH.md (Pattern 4: Fire-and-Forget Auto-Title; Open Question 3; Open Question 4),
    packages/client/src/cyberharness/session/store.py,
    packages/client/tests/unit/session/test_manager.py (RED stubs from 02-01)
  </read_first>
  <behavior>
    - `SessionMgr(store: SessionStore, titler: Titler)` — titler is an injected protocol/callable, defaults to the stub
    - `async def add_user_turn(loaded: LoadedSession, content: str) -> None` — appends Message(role="user", content, timestamp=utcnow()), calls `await store.save(loaded)`. Returns after save completes — the router MUST NOT be invoked until this returns (D-07)
    - `async def add_assistant_turn(loaded: LoadedSession, content: str, model_id: str, tokens_in: int, tokens_out: int, latency_ms: int) -> None` — appends assistant Message with `model_id` (alias `_model` on disk), appends ModelCall, saves; if this is the first assistant message AND `loaded.session.title is None`, schedules `asyncio.create_task(self._title_and_persist(loaded))` — task stored on `self._bg_tasks: set[asyncio.Task]`, `task.add_done_callback(self._bg_tasks.discard)` (Pitfall 11)
    - `async def shutdown() -> None` — cancels all `_bg_tasks`, `await asyncio.gather(*, return_exceptions=True)`
    - `titler.py`: `async def generate_title(session: Session) -> str` returns `session.messages[0].content[:50].strip()` — Phase 2 stub, no Ollama
    - test_manager.py GREEN cases: test_add_user_turn_saves_before_return (patch `store.save` and assert it was awaited before `add_user_turn` returns), test_user_turn_persisted_before_router (order assertion via mock), test_multi_turn_accumulates (append 3 pairs, assert `loaded.session.messages` length is 6 in order), test_auto_title_scheduled_after_first_assistant_only (second assistant call does not schedule another task), test_shutdown_cancels_bg_tasks (create task with sleep, call shutdown, assert task is cancelled)
  </behavior>
  <action>
    Implement `SessionMgr` per RESEARCH.md Pattern 4. Titler is a Protocol: `class Titler(Protocol): async def generate(self, session: Session) -> str: ...`. Provide `StubTitler` class implementing this in `titler.py` — this is the Phase 2 default. Do NOT call Ollama from `titler.py` in Phase 2 — Ollama wiring lands in Phase 3 (per RESEARCH.md Open Question 3). `_title_and_persist` MUST catch `asyncio.CancelledError` and re-raise, and catch general `Exception` and `logger.exception(...)` without re-raising (Pitfall 6: cancelled title is best-effort). The bg task set MUST use `task.add_done_callback(self._bg_tasks.discard)` — RESEARCH.md notes this is the standard Pitfall-11 mitigation. Do NOT use `asyncio.Lock` at the SessionMgr level — the per-session save serialisation lives on `LoadedSession.save_lock` inside `store.save` (Task 2 of 02-02 established this). Export `SessionMgr`, `StubTitler`, `Titler` from `session/__init__.py`.
  </action>
  <verify>
    <automated>uv run --package cyberharness pytest packages/client/tests/unit/session/test_manager.py -x -q</automated>
  </verify>
  <done>
    test_manager.py GREEN. `grep -q "add_done_callback" packages/client/src/cyberharness/session/manager.py` succeeds. Auto-title stub is not making any HTTP call (grep for `httpx`/`ollama` returns nothing in titler.py).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ResumeScreen + boot sequence wiring — SESS-03 end-to-end</name>
  <files>
    packages/client/src/cyberharness/tui/screens/resume.py,
    packages/client/src/cyberharness/tui/app.py,
    packages/client/tests/unit/session/test_resume.py
  </files>
  <read_first>
    .planning/phases/02-session-manager/02-RESEARCH.md (Pattern 3: ModalScreen Return Value; D-13/14/15/16/17),
    packages/client/src/cyberharness/tui/app.py (from Phase 1),
    packages/client/src/cyberharness/tui/screens/ (WizardScreen — the reference ModalScreen pattern from Phase 1),
    packages/client/tests/unit/session/test_resume.py (RED stubs)
  </read_first>
  <behavior>
    - `ResumeResult = TypedDict("ResumeResult", {"action": Literal["resume","fresh"], "session_id": str | None})`
    - `ResumeScreen(ModalScreen[ResumeResult])` — constructor takes `sessions: list[LoadedSession]`; composes a Label header ("Resume an interrupted session? (N found)"), a ListView with items formatted as `"[i+1] <title or 'Untitled session'> (<len(messages)> turns, <humanize_ago(updated_at)>)"` (D-14), and a "Start fresh" Button
    - `on_list_view_selected` handler calls `self.dismiss({"action": "resume", "session_id": str(self._sessions[idx].session.id)})` — plain method call, no `await` (RESEARCH.md Anti-Pattern; Textual raises ScreenError otherwise)
    - `on_button_pressed` for the Start fresh button calls `self.dismiss({"action": "fresh", "session_id": None})`
    - `tui/app.py` boot: `on_mount` calls `sessions = await self.session_store.scan_active()`; zero → `_start_new_session()` directly (D-16); one or more → `self.push_screen(ResumeScreen(sessions), self._handle_resume_choice)` (D-17)
    - `_handle_resume_choice(result)` — if `result["action"] == "fresh"`, call `await self.session_store.abandon_all(sessions)` then `_start_new_session()`; if `"resume"`, use the LoadedSession from the passed list matching `session_id` (do NOT reload from disk — the store already holds the filelock)
    - test_resume.py GREEN cases (using `App.run_test` pilot): test_zero_sessions_skips_resume (scan returns []; pilot never sees ResumeScreen), test_one_session_still_prompts (scan returns 1; ResumeScreen is on screen stack), test_multiple_sessions_ordered_recent_first (already covered in store, but re-assert render text), test_fresh_dismisses_with_fresh_action (click button, assert callback got {"action":"fresh"}), test_resume_dismisses_with_session_id (click first ListItem, assert callback got matching UUID), test_fresh_abandons_all_listed (mock abandon_all, click fresh, assert called with full list — D-15/Pitfall 4)
  </behavior>
  <action>
    Implement `ResumeScreen` per RESEARCH.md Pattern 3. `humanize_ago(dt)` — accept "2 hours ago"-style or absolute ISO-8601; per CONTEXT.md Claude's Discretion, prefer humanised for readability; small helper in resume.py, no new deps. Wire `tui/app.py` boot in the existing `on_mount` (Phase 1 already established this hook for WizardScreen); the sequence is: config-loaded gate → wizard? → scan_active → resume? → main. If `SessionStore` is not yet attached to `App` (i.e., Phase 1 didn't create it), construct it in `on_mount` using `Paths.sessions_dir` from Phase 1 and store as `self.session_store`. `_start_new_session` calls `self.session_store.create()` and stores the LoadedSession as `self.loaded_session`. For `App.run_test` in tests: use `async with App().run_test() as pilot:` per Textual docs; assert screen stack via `app.screen_stack` and `isinstance(app.screen, ResumeScreen)`. The test harness must pre-populate the sessions directory via `tmp_sessions_dir` fixture from conftest.py (02-01). Do NOT `await self.dismiss(...)` anywhere — this is a correctness constraint.
  </action>
  <verify>
    <automated>uv run --package cyberharness pytest packages/client/tests/unit/session/test_resume.py -x -q</automated>
    <automated>uv run --package cyberharness pytest packages/client/tests/unit/session/ -x -q</automated>
  </verify>
  <done>
    test_resume.py GREEN. Full `pytest packages/client/tests/unit/session/` suite GREEN. `grep -c "await self.dismiss" packages/client/src/cyberharness/tui/screens/resume.py` returns 0. `grep -q "add_done_callback\|_bg_tasks" packages/client/src/cyberharness/session/manager.py` succeeds. Session-manager phase end-to-end acceptance: history accumulates (SESS-01), atomic write + filelock (SESS-02), resume prompt honours user choice including "Start fresh" abandons all (SESS-03).
  </done>
</task>

</tasks>

<verification>
  <automated>uv run --package cyberharness pytest packages/client/tests/unit/session/ -x -q</automated>
  <automated>uv run --package cyberharness pytest packages/client/tests/ -x -q</automated>
  <automated>grep -c "await self.dismiss" packages/client/src/cyberharness/tui/screens/resume.py | grep -q "^0$"</automated>
  <automated>grep -q "abandon_all" packages/client/src/cyberharness/tui/app.py</automated>
</verification>

<success_criteria>
- SESS-03 end-to-end: zero sessions → no ResumeScreen; one+ sessions → ResumeScreen; resume loads picked session; fresh abandons ALL listed
- SessionMgr: user turn saved before router call; auto-title fires once after first assistant reply; shutdown cancels bg tasks cleanly
- Titler is a Phase-2 stub with no Ollama dependency (Phase 3 replaces it)
- Full Phase 2 test suite GREEN — SESS-01, SESS-02, SESS-03 all satisfied
</success_criteria>

<output>
Create `.planning/phases/02-session-manager/02-03-SUMMARY.md` when done
</output>
