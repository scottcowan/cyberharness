---
phase: 02-session-manager
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/tests/unit/session/__init__.py
  - packages/client/tests/unit/session/conftest.py
  - packages/client/tests/unit/session/test_models.py
  - packages/client/tests/unit/session/test_store.py
  - packages/client/tests/unit/session/test_manager.py
  - packages/client/tests/unit/session/test_resume.py
  - packages/client/src/cyberharness/session/__init__.py
  - packages/client/src/cyberharness/session/models.py
autonomous: true
requirements: [SESS-01]

must_haves:
  truths:
    - "Session accumulates a `list[Message]` across turns and preserves message order (SESS-01 model layer)"
    - "`Session.model_dump_json(by_alias=True)` produces JSON with a literal `_model` key, and `Session.model_validate_json(...)` round-trips losslessly (D-18, D-19)"
    - "`extra=\"forbid\"` on Message/ModelCall/Session rejects unknown keys with pydantic ValidationError (Pitfall 7 quarantine surface)"
    - "Wave 0 test files exist for models/store/manager/resume; the suite exits non-zero because store/manager/resume implementations do not yet exist (RED)"
  artifacts:
    - path: "packages/client/tests/unit/session/conftest.py"
      provides: "tmp_sessions_dir factory + session_factory fixture used by all Phase 2 tests"
    - path: "packages/client/tests/unit/session/test_models.py"
      provides: "GREEN once Task 2 lands — includes test_model_alias_roundtrip and extra=forbid rejection"
      contains: "test_model_alias_roundtrip"
    - path: "packages/client/tests/unit/session/test_store.py"
      provides: "RED stub tests importing SessionStore, atomic_write, LoadedSession — will fail on ImportError until 02-02"
      contains: "from cyberharness.session.store import"
    - path: "packages/client/tests/unit/session/test_manager.py"
      provides: "RED stub tests importing SessionMgr — will fail on ImportError until 02-03"
      contains: "from cyberharness.session.manager import"
    - path: "packages/client/tests/unit/session/test_resume.py"
      provides: "RED stub tests importing ResumeScreen + ResumeResult — will fail on ImportError until 02-03"
      contains: "from cyberharness.tui.screens.resume import"
    - path: "packages/client/src/cyberharness/session/models.py"
      provides: "Message, ModelCall, Session pydantic v2 models per D-18"
      contains: "class Session"
      exports: ["Message", "ModelCall", "Session"]
    - path: "packages/client/src/cyberharness/session/__init__.py"
      provides: "package exports for Message, ModelCall, Session"
      contains: "Message"
  key_links:
    - from: "packages/client/src/cyberharness/session/models.py"
      to: "packages/client/tests/unit/session/test_models.py"
      via: "pydantic v2 alias roundtrip (`_model` on disk ↔ `model_id` in code)"
      pattern: "alias=\"_model\""
---

<objective>
Land Wave 0 test stubs for every Phase 2 module (models, store, manager, resume) and implement the pydantic v2 session models (Message, ModelCall, Session) that satisfy `test_models.py` GREEN. This is the SESS-01 model foundation — messages accumulate as `list[Message]` and round-trip through `model_dump_json` / `model_validate_json` with the `_model` alias preserved.

Purpose: Establishes RED state for 02-02 and 02-03 (their tests exist and fail on ImportError) and delivers the shared data contract every downstream Phase 2 plan consumes.

Output: `pytest tests/unit/session/test_models.py -x` GREEN; `pytest tests/unit/session/ -x` exits non-zero on ImportError for store/manager/resume modules (expected RED).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-session-manager/02-CONTEXT.md
@.planning/phases/02-session-manager/02-RESEARCH.md
@.planning/phases/02-session-manager/02-VALIDATION.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@docs/session-design.md
@packages/client/tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 test stubs for models, store, manager, resume</name>
  <files>
    packages/client/tests/unit/session/__init__.py,
    packages/client/tests/unit/session/conftest.py,
    packages/client/tests/unit/session/test_models.py,
    packages/client/tests/unit/session/test_store.py,
    packages/client/tests/unit/session/test_manager.py,
    packages/client/tests/unit/session/test_resume.py
  </files>
  <read_first>
    packages/client/tests/conftest.py,
    .planning/phases/02-session-manager/02-VALIDATION.md,
    .planning/phases/02-session-manager/02-RESEARCH.md (sections: Phase Requirements → Test Map, Wave 0 Gaps)
  </read_first>
  <behavior>
    - conftest.py exposes `tmp_sessions_dir(tmp_path)` returning a Path with `active/` and `archive/` subdirs pre-created (mode 0o700)
    - conftest.py exposes `session_factory(id=None, title=None, messages=None, workspace_id="default")` returning a `Session` instance with tz-aware `created_at`/`updated_at`
    - test_models.py: test_message_defaults, test_model_alias_roundtrip (dumps `_model` key, reloads to `model_id` attr), test_extra_forbid_rejects_unknown_keys, test_session_accumulates_messages_in_order, test_utc_timestamps_sort_iso8601
    - test_store.py: RED — imports SessionStore, LoadedSession, atomic_write_json from cyberharness.session.store; tests defined but assert `pytest.fail("02-02 not landed")` or rely on ImportError
    - test_manager.py: RED — imports SessionMgr from cyberharness.session.manager
    - test_resume.py: RED — imports ResumeScreen, ResumeResult from cyberharness.tui.screens.resume
    - Every RED stub file MUST use `import`-time failure OR explicit `pytest.fail(...)`, never `pytest.skip` (Nyquist: no false green)
  </behavior>
  <action>
    Create the six files above. `conftest.py` MUST NOT import from `cyberharness.session.store` or `cyberharness.session.manager` (those don't exist yet); build the `session_factory` fixture using only `cyberharness.session.models.Session` (available after Task 2 — this task can leave `session_factory` as a lazy import inside the fixture body). `test_models.py` MUST include an assertion that `Session(...).model_dump_json(by_alias=True)` contains the substring `"_model"` when a Message has `model_id` set, and that `Session.model_validate_json(dumped).messages[0].model_id` equals the original — this is the A3 assumption verification. Do NOT set timestamps via `datetime.now()` — always use `datetime.now(timezone.utc)` (Pitfall 3). No fenced code inside this action; see RESEARCH.md Code Examples section for the exact model config.
  </action>
  <verify>
    <automated>uv run --package cyberharness pytest packages/client/tests/unit/session/ -x -q 2>&1 | grep -E "ImportError|collected|failed"</automated>
  </verify>
  <done>
    Six files exist. `uv run pytest packages/client/tests/unit/session/ -x` exits non-zero (RED — models file not yet created, store/manager/resume imports fail). test_models.py, test_store.py, test_manager.py, test_resume.py each contain at least one function starting with `test_`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Session pydantic models — Message, ModelCall, Session</name>
  <files>
    packages/client/src/cyberharness/session/models.py,
    packages/client/src/cyberharness/session/__init__.py
  </files>
  <read_first>
    .planning/phases/02-session-manager/02-RESEARCH.md (Code Examples: Session Models section — lines discussing D-18/D-19),
    .planning/phases/02-session-manager/02-CONTEXT.md (D-18, D-19),
    packages/client/tests/unit/session/test_models.py (created in Task 1 — these are the target)
  </read_first>
  <behavior>
    - Message: role Literal["user","assistant","system"]; content str; timestamp datetime; model_id str|None with alias="_model"; ConfigDict(extra="forbid", populate_by_name=True)
    - ModelCall: model_id str; tokens_in int; tokens_out int; latency_ms int; timestamp datetime; ConfigDict(extra="forbid")
    - Session: id UUID (default_factory=uuid4); workspace_id str = "default"; title str|None = None; state Literal["active","complete","abandoned"] = "active"; messages list[Message] = []; model_log list[ModelCall] = []; created_at datetime; updated_at datetime; ConfigDict(extra="forbid")
    - `Session.model_dump_json(by_alias=True)` emits `"_model"` when Message.model_id is set (D-19, A3 verification)
    - `Session.model_validate_json(...)` accepts both `_model` (on-disk) and `model_id` (in-code) keys thanks to populate_by_name
  </behavior>
  <action>
    Implement per D-18 (models.py) using pydantic v2 `BaseModel` + `ConfigDict`. Use `Field(default=None, alias="_model")` on the Message.model_id attribute (NOT a private-named `_model` field — pydantic v2 treats leading-underscore names as private attributes and will silently drop them; this is the A3 assumption). Set `model_config = ConfigDict(extra="forbid", populate_by_name=True)` on Message. Set `extra="forbid"` on ModelCall and Session as well — nested models do NOT inherit ConfigDict (per RESEARCH.md notes). Add `Message`, `ModelCall`, `Session` to session/__init__.py `__all__`. Do NOT add any I/O to models.py — pure data (per Architectural Responsibility Map: Core tier, no I/O). Do NOT add `state=\"draining\"` or `state=\"queued\"` to the Literal — Phase 2 states are `active|complete|abandoned` per D-11.
  </action>
  <verify>
    <automated>uv run --package cyberharness pytest packages/client/tests/unit/session/test_models.py -x -q</automated>
  </verify>
  <done>
    test_models.py passes GREEN including test_model_alias_roundtrip. `python -c "from cyberharness.session import Message, ModelCall, Session; s = Session(created_at=__import__('datetime').datetime.now(__import__('datetime').timezone.utc), updated_at=__import__('datetime').datetime.now(__import__('datetime').timezone.utc)); print(s.model_dump_json(by_alias=True))"` runs without error and prints a JSON blob. Store/manager/resume tests still RED (expected — those land in 02-02/02-03).
  </done>
</task>

</tasks>

<verification>
  <automated>uv run --package cyberharness pytest packages/client/tests/unit/session/test_models.py -x -q</automated>
  <automated>grep -q "alias=\"_model\"" packages/client/src/cyberharness/session/models.py</automated>
  <automated>grep -q "extra=\"forbid\"" packages/client/src/cyberharness/session/models.py</automated>
  <automated>test -f packages/client/tests/unit/session/test_store.py && test -f packages/client/tests/unit/session/test_manager.py && test -f packages/client/tests/unit/session/test_resume.py</automated>
</verification>

<success_criteria>
- test_models.py GREEN; `_model` alias roundtrip test explicitly asserts on-disk key
- Wave 0 stub files exist for store, manager, resume — RED on ImportError (they will be turned GREEN by 02-02 and 02-03)
- No leading-underscore field names in models.py (pydantic v2 A3 requirement)
- `extra="forbid"` present on all three model classes
</success_criteria>

<output>
Create `.planning/phases/02-session-manager/02-01-SUMMARY.md` when done
</output>
