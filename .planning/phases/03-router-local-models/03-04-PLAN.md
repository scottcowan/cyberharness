---
phase: 03-router-local-models
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - packages/client/src/cyberharness/router/router.py
  - packages/client/src/cyberharness/router/registry.py
  - packages/client/src/cyberharness/router/__init__.py
  - packages/client/tests/unit/router/test_router.py
autonomous: true
requirements: [ROUT-02]

must_haves:
  truths:
    - "Router.route(messages) is an async generator that dispatches to LocalModelClient.stream() for all turns in v1.0 (ROUT-02, D-17)"
    - "Router.route() reads probe.is_connected once at entry (never mid-stream) — Pitfall 8 guard"
    - "Router.route() calls to_wire(messages) exactly once before dispatch — the single wire-sanitisation choke point (D-09)"
    - "When policy resolves to 'cloud', Router.route() raises RelayNotImplementedError BEFORE any `yield` — the exception fires when iteration starts, not silently after some chunks (Pitfall 10)"
    - "phase_registry.policy_for() returns 'local' for every input in Phase 3 (D-17 — cloud is stubbed)"
    - "The Router is stateless — no per-request state on the instance; safe to concurrent-call from multiple sessions"
  artifacts:
    - path: "packages/client/src/cyberharness/router/registry.py"
      provides: "phase_registry: policy_for(phase: str | None) -> Literal['local', 'cloud']"
      contains: "def policy_for"
      exports: ["policy_for", "PHASE_POLICIES"]
    - path: "packages/client/src/cyberharness/router/router.py"
      provides: "Router class with async route(messages) generator"
      contains: "class Router"
      exports: ["Router"]
  key_links:
    - from: "packages/client/src/cyberharness/router/router.py"
      to: "packages/client/src/cyberharness/router/local_client.py"
      via: "async for chunk in self.local.stream(wire_messages): yield chunk"
      pattern: "self.local.stream"
    - from: "packages/client/src/cyberharness/router/router.py"
      to: "packages/client/src/cyberharness/router/wire.py"
      via: "to_wire(messages) called once at route() entry"
      pattern: "to_wire"
    - from: "packages/client/src/cyberharness/router/router.py"
      to: "packages/client/src/cyberharness/probe/probe.py"
      via: "self.probe.is_connected read once at route() entry — never inside the stream loop"
      pattern: "self.probe.is_connected"
---

<objective>
Land the `Router` — the stateless dispatch that ties Phase 3 together. It calls `to_wire()` once, reads `probe.is_connected` once, resolves the routing policy via `phase_registry`, and either yields chunks from `LocalModelClient.stream()` or raises `RelayNotImplementedError` — the latter placed *before* any `yield` so it fires the moment the caller iterates (Pitfall 10).

Purpose: This is ROUT-02 complete — the phase's user-visible outcome: "every turn dispatches to a local backend; the relay path exists as a stub but is never invoked in normal use." Also delivers the phase_registry infrastructure that v1.1 will extend when the relay ships.

Output: `pytest tests/unit/router/test_router.py -x` GREEN, and the full Phase 3 suite (`pytest tests/unit/router/ tests/unit/probe/ -x`) green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-router-local-models/03-CONTEXT.md
@.planning/phases/03-router-local-models/03-RESEARCH.md
@.planning/phases/03-router-local-models/03-VALIDATION.md
@packages/client/src/cyberharness/router/wire.py
@packages/client/src/cyberharness/router/errors.py
@packages/client/src/cyberharness/router/local_client.py
@packages/client/src/cyberharness/probe/probe.py
@packages/client/tests/unit/router/test_router.py
@packages/client/tests/unit/router/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement phase_registry and Router with pre-yield NotImplementedError guard</name>
  <files>
    packages/client/src/cyberharness/router/router.py,
    packages/client/src/cyberharness/router/registry.py,
    packages/client/src/cyberharness/router/__init__.py
  </files>
  <read_first>
    .planning/phases/03-router-local-models/03-RESEARCH.md (Code Examples: Router — full skeleton; Pitfall 10 NotImplementedError inside an AsyncIterator; Anti-Patterns to Avoid),
    .planning/phases/03-router-local-models/03-CONTEXT.md (D-01, D-15, D-17, D-18),
    packages/client/src/cyberharness/router/wire.py,
    packages/client/src/cyberharness/router/errors.py,
    packages/client/src/cyberharness/router/local_client.py,
    packages/client/src/cyberharness/probe/probe.py,
    packages/client/tests/unit/router/test_router.py
  </read_first>
  <behavior>
    - `registry.py`: `PHASE_POLICIES: dict[str, Literal["local", "cloud"]]` — empty dict in Phase 3 (no phase-based routing until v1.1). `def policy_for(phase: str | None = None) -> Literal["local", "cloud"]` returns `"local"` unconditionally in Phase 3. Signature keeps the `phase` parameter for v1.1 forward-compat (does not remove it — v1.1 will implement lookup).
    - `router.py`: `class Router` constructor: `Router(config, local: LocalModelClient, probe: Probe)`. Stores refs; no other state.
    - `async def route(self, messages: list[Message], phase: str | None = None) -> AsyncIterator[str]`:
      1. `wire_messages = to_wire(messages)` — single choke point.
      2. `_is_connected = self.probe.is_connected` — read once, commit for the stream (Pitfall 8).
      3. `policy = policy_for(phase)`.
      4. **If `policy == "cloud"`**: `raise RelayNotImplementedError("relay not implemented — v1.1")`. This raise MUST appear before any `yield` in the function body. Since the function contains at least one `yield` (in the local branch), the compiler still treats `route` as an async generator; the raise fires on the *first* `__anext__()` call, satisfying Pitfall 10.
      5. **Local branch**: `async for chunk in self.local.stream(wire_messages): yield chunk`.
    - `__init__.py` re-exports `Router` alongside the existing exports.
  </behavior>
  <action>
    Implement per RESEARCH.md § Code Examples — Router skeleton, with the critical structural detail from Pitfall 10 spelled out: the `raise` must textually precede every `yield` in `route()`, and the function must contain at least one `yield` so Python treats it as an async generator. The simplest shape is:
    ```
    async def route(self, messages, phase=None):
        wire_messages = to_wire(messages)
        _is_connected = self.probe.is_connected  # read once
        policy = policy_for(phase)
        if policy == "cloud":
            raise RelayNotImplementedError("relay not implemented — v1.1")
        async for chunk in self.local.stream(wire_messages):
            yield chunk
    ```
    But since `<action>` MUST NOT contain fenced code, express the structure via prose: (a) call `to_wire` at line 1 of body, (b) snapshot `probe.is_connected` at line 2, (c) resolve policy at line 3, (d) `if policy == "cloud": raise RelayNotImplementedError(...)` before any yield, (e) `async for chunk in self.local.stream(wire_messages): yield chunk`. Do NOT read `self.probe.is_connected` again inside the loop (Pitfall 8). Do NOT create a new httpx client here — reuse `self.local` (D-05). Do NOT catch RelayNotImplementedError — it MUST propagate to the caller. `phase` parameter is present for forward-compat with v1.1 but unused in the routing decision today (`policy_for` doesn't consult it in Phase 3). The stored `_is_connected` local is not consumed in Phase 3 routing (D-17: always local), but is assigned to make Pitfall 8 discipline visible in the code — leave it as a local named `_is_connected` so grep can find the discipline.
  </action>
  <verify>
    <automated>uv run --package cyberharness pytest packages/client/tests/unit/router/test_router.py -x -q</automated>
    <automated>uv run --package cyberharness pytest packages/client/tests/unit/router/ packages/client/tests/unit/probe/ -x -q</automated>
    <automated>python3 -c "import re, sys; src = open('packages/client/src/cyberharness/router/router.py').read(); body = re.search(r'async def route.*?(?=\n(?:    )?(?:async )?def |\Z)', src, re.DOTALL).group(0); raise_pos = body.find('raise RelayNotImplementedError'); yield_pos = body.find('yield'); assert raise_pos != -1 and yield_pos != -1 and raise_pos < yield_pos, f'raise at {raise_pos} must precede yield at {yield_pos}'; print('OK: raise before yield')"</automated>
    <automated>grep -q "to_wire" packages/client/src/cyberharness/router/router.py</automated>
    <automated>grep -q "self.probe.is_connected" packages/client/src/cyberharness/router/router.py</automated>
    <automated>grep -q "policy_for" packages/client/src/cyberharness/router/registry.py</automated>
  </verify>
  <done>
    test_router.py GREEN: test_dispatch_local passes (Router yields chunks from a mocked `LocalModelClient.stream`), test_cloud_stub_raises passes (monkeypatching `policy_for` or `PHASE_POLICIES` to return "cloud" causes `async for _ in router.route(msgs): ...` to raise `RelayNotImplementedError` on first iteration — matches Pitfall 10 test pattern from RESEARCH). The grep gate confirming `raise` textually precedes `yield` inside `route()` passes. Entire Phase 3 unit-test suite green: `pytest packages/client/tests/unit/router/ packages/client/tests/unit/probe/ -x` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller (Phase 2 SessionMgr, Phase 5 TUI) → Router | Router receives `list[Message]` — must not trust that caller stripped `_model` (D-09 says to_wire is the single choke point right here, at the Router boundary). |
| Router → LocalModelClient | Internal boundary; already-sanitised `list[dict]` flows across. LocalModelClient never sees raw Message objects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Tampering | `_model` field leaking to wire | mitigate | `to_wire(messages)` invoked at Router.route() entry — single choke point per D-09. Guaranteed by 03-01 test_wire.py contract. |
| T-03-16 | Denial of Service / Elevation | Cloud policy silently falling through to local dispatch (or vice versa) | mitigate | `RelayNotImplementedError` raised *before* any `yield` per Pitfall 10; grep-verified in acceptance criteria. A caller that requests cloud MUST see the error immediately, not receive local completions labelled as cloud. |
| T-03-17 | Tampering | Router state (probe.is_connected) changing mid-stream and altering dispatch mid-response | mitigate | Read `probe.is_connected` once at `route()` entry into a local; never re-consulted (Pitfall 8, D-15). |
| T-03-18 | Elevation of Privilege | Callers registering tools with `name` parameter (Ollama bug #16932) to trigger silent tool-call drops | mitigate | Registration-time guard in ToolRegistry (from 03-01). Router itself does not participate — the guard fires at registration, before any Router.route() call. |
</threat_model>

<verification>
  <automated>uv run --package cyberharness pytest packages/client/tests/unit/router/ packages/client/tests/unit/probe/ -x -q</automated>
  <automated>python3 -c "import re, sys; src = open('packages/client/src/cyberharness/router/router.py').read(); body = re.search(r'async def route.*?(?=\n(?:    )?(?:async )?def |\Z)', src, re.DOTALL).group(0); raise_pos = body.find('raise RelayNotImplementedError'); yield_pos = body.find('yield'); assert raise_pos != -1 and yield_pos != -1 and raise_pos < yield_pos, 'raise must precede yield'; print('OK')"</automated>
  <automated>grep -q "to_wire" packages/client/src/cyberharness/router/router.py</automated>
  <automated>grep -q "policy_for" packages/client/src/cyberharness/router/router.py</automated>
  <automated>grep -v '^#' packages/client/src/cyberharness/router/router.py | grep -c "self.probe.is_connected" | { read n; test "$n" = "1"; }</automated>
</verification>

<success_criteria>
- test_router.py GREEN (dispatch_local + cloud_stub_raises)
- Full Phase 3 unit-test suite GREEN
- `raise RelayNotImplementedError` textually precedes any `yield` in Router.route (grep-verified)
- `probe.is_connected` read exactly once in router.py source (grep-verified)
- `to_wire()` invoked in router.py (grep-verified)
- `Router` importable from `cyberharness.router`
</success_criteria>

<output>
Create `.planning/phases/03-router-local-models/03-04-SUMMARY.md` when done
</output>
